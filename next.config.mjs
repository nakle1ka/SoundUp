/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.mds.yandex.net', 'i.pinimg.com', 'avatars.yandex.net'],
        
    },
};

export default nextConfig;
